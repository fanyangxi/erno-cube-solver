pub mod CrossSolver {
    use std::collections::HashMap;

    pub fn aaa () {
        let aaa: HashMap<String, [String]> = [
            ("U", [])
        ].iter().cloned().collect();

        let steps: HashMap<String, HashMap<String, [String]>> = HashMap::new();
        steps.insert("U", aaa)
    }
}
